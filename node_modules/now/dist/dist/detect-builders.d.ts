import { Builder, BuilderFunctions, DetectorResult } from './types';
interface ErrorResponse {
    code: string;
    message: string;
}
interface Options {
    tag?: 'canary' | 'latest' | string;
    functions?: BuilderFunctions;
}
export declare function getIgnoreApiFilter(optionsOrBuilders: Options | Builder[]): (file: string) => boolean;
export declare function sortFiles(fileA: string, fileB: string): number;
export declare function detectBuilders(files: string[], detectorResult?: Partial<DetectorResult> | null, options?: Options): Promise<{
    builders: Builder[] | null;
    errors: ErrorResponse[] | null;
    warnings: ErrorResponse[];
}>;
export {};
