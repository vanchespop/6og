import { Detector, DetectorParameters, DetectorResult } from '../types';
export declare const pkgDetectors: Detector[];
export declare const detectors: Detector[];
export declare function firstTruthy<T>(promises: Promise<T>[]): Promise<T>;
export declare function detectDefaults(params: DetectorParameters): Promise<DetectorResult>;
