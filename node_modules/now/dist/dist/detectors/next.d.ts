import { DetectorParameters, DetectorResult } from '../types';
export default function detectNext({ fs: { getPackageJsonBuildCommand, getDependencyVersion }, }: DetectorParameters): Promise<DetectorResult>;
