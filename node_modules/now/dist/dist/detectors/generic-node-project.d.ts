import { DetectorParameters, DetectorResult } from '../types';
export default function detectGenericNodeProject({ fs: { isNpm, getPackageJsonCommand }, }: DetectorParameters): Promise<DetectorResult>;
