import { DetectorParameters, DetectorResult } from '../types';
export default function detectEmber({ fs: { getPackageJsonBuildCommand, getDependencyVersion }, }: DetectorParameters): Promise<DetectorResult>;
