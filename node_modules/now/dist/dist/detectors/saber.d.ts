import { DetectorParameters, DetectorResult } from '../types';
export default function detectSaber({ fs: { getPackageJsonBuildCommand, getDependencyVersion }, }: DetectorParameters): Promise<DetectorResult>;
